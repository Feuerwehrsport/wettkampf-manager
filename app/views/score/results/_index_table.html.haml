- if score_results.count .zero?
  .alert.alert-info Keine Einträge gefunden
- else
  %table.table
    %tr
      %th= Score::Result.human_attribute_name(:assessment)
      %th
    - score_results.each do |score_result|
      %tr
        %td
          = discipline_image(score_result.assessment.discipline)
          = score_result.to_s
        %td.text-right
          = btn_link_to 'Ansehen', score_result
