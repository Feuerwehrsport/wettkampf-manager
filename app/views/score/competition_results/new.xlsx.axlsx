wb = xlsx_package.workbook
wb.add_worksheet(name: "weiblich - Gesamtwertung") do |sheet|
  table_data(:female, @female).each do |row|
    sheet.add_row row
  end
end
wb.add_worksheet(name: "männlich - Gesamtwertung") do |sheet|
  table_data(:male, @male).each do |row|
    sheet.add_row row
  end
end
