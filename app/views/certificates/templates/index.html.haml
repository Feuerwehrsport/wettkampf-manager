.row
  .col-md-8
    = box Certificates::Template.model_name.human(count: 0) do |box|
      %table.table
        %tr
          %th Name
          %th
        - @certificates_templates.each do |template|
          %tr
            %td= template.name
            %td.text-right
              = link_to "Ansehen", {action: :show, id: template}, class: "btn btn-sm btn-default"
              = link_to "Bearbeiten", {action: :edit, id: template}, class: "btn btn-sm btn-default"
  .col-md-4
    = box '' do |box|
      %p Hier können Sie Vorlagen für Urkunden anlegen, die später mit den Ergebnislisten automatisch ausgefüllt werden.
      %p.bg-danger Bitte beachten Sie, dass diese Funktionen nur mit Firefox und Chrome ausführlich getestet wurden.
      - box.footer do
        = link_to("Hinzufügen", { action: :new }, class: "btn btn-info")