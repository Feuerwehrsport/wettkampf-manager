= simple_form_for @discipline do |f|
  = box Discipline.model_name.human do |box|
    = f.error_notification
    = f.input :type, collection: Discipline.types.map { |k| [k.model_name.human, k.name] }
    = f.input :name
    = f.input :short_name
    - box.footer do
      = cancel_link
      = f.primary_button :submit, "Speichern"
