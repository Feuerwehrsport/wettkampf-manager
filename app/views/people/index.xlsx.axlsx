wb = xlsx_package.workbook

if @female.present?
  wb.add_worksheet(name: 'Frauen'.truncate(30)) do |sheet|
    index_export_data(@female).each do |row|
      sheet.add_row row
    end
  end
end

if @male.present?
  wb.add_worksheet(name: 'Männer'.truncate(30)) do |sheet|
    index_export_data(@male).each do |row|
      sheet.add_row row
    end
  end
end
